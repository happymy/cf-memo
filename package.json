{
  "name": "cf-memo",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.0.0"
  }
}
